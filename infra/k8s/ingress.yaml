apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: t79-ingress
  namespace: t79
  annotations:
    # Ingress controller (adjust if you're using something else)
    kubernetes.io/ingress.class: nginx

    # Optional: if you use cert-manager for TLS
    # cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - t79.yourdomain.com
      secretName: t79-tls # create this via cert-manager or manually
  rules:
    - host: t79.yourdomain.com
      http:
        paths:
          # Frontend (Next.js)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: t79-frontend
                port:
                  number: 3000

          # Backend API (FastAPI)
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: t79-backend
                port:
                  number: 8000
