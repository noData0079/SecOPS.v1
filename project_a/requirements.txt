# Project A (AI model) dependencies. These mirror the consolidated root requirements
# so environments stay in sync between the model and the FastAPI app.
torch==2.3.1
transformers==4.44.2
vllm==0.5.4.post1
numpy==1.26.4
Pillow==10.4.0
