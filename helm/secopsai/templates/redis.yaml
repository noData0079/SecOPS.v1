{{- if .Values.redis.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: secops-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: secops-redis
  template:
    metadata:
      labels:
        app: secops-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: redis-data
              mountPath: /data
      volumes:
        - name: redis-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: secops-redis
spec:
  selector:
    app: secops-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
{{- end }}
