apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: t79-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
    - hosts:
        - {{ .Values.domain }}
      secretName: t79ai-tls
  rules:
    - host: {{ .Values.domain }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: t79-backend
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: t79-frontend
                port:
                  number: 3000
