apiVersion: apps/v1
kind: Deployment
metadata:
  name: t79-backend
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      app: t79-backend
  template:
    metadata:
      labels:
        app: t79-backend
    spec:
      containers:
        - name: backend
          image: {{ .Values.backend.image }}
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: t79-secrets
                  key: DATABASE_URL
            - name: SUPABASE_URL
              value: {{ .Values.supabase.url }}
            - name: SUPABASE_ANON_KEY
              value: {{ .Values.supabase.anon_key }}
            - name: REDIS_URL
              value: redis://t79-redis:6379/0
          ports:
            - containerPort: 8000
